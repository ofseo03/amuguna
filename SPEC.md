# amuguna — 서비스 스펙

> 개인 프로필 기반 공공 금융정보 매칭 서비스
> 2026 금융 AI Challenge (금융보안원 주최 / 금융위원회 후원 / 운영 데이콘) 출품작

---

## 0. 대회 요건 (근거)

| 항목 | 내용 |
|---|---|
| 대회명 | 2026 금융 AI Challenge — 상상을 넘어 실제로, AI로 움직이는 금융의 미래 |
| 주제 | AI 기반의 금융 현안 해결 아이디어 및 웹서비스 개발 |
| 설명 | **금융소비자 특성과 서비스 채널을 고려한 맞춤형 금융 AI 서비스**를 기획하고 실제 작동하는 웹서비스로 구현 |
| 해당 세부주제 예시 | "청년층 자산 형성 및 고령층·장애인 디지털 격차 해소를 위한 AI 기반 포용적 금융서비스" |
| 제공 데이터 | **없음** (외부 데이터 사용 가능, 단 사용 사실 공개 의무) |
| 팀 규모 | 1~4인 |
| 총상금 | 3,500만원 (대상 2,000 / 최우수 500 / 우수 각 200) |

### 마감 일정

| 단계 | 기한 |
|---|---|
| 기획서(PDF) 제출 | 2026-09-07(월) 10:00 |
| MVP 산출물 제출 — 기능명세서(PDF) + **웹서비스 URL** | 2026-09-07(월) 10:00 |
| **서비스 가용 필수 구간** | 2026-09-07 11:00 ~ 09-11 23:59 (미접속 시 **결격**) |
| 발표심사 대상 발표 (11팀 내외) | 2026-09-22 |
| 최종 산출물 (발표자료 PDF + 소스 ZIP, dacon@dacon.io) | 2026-10-08 23:59 |
| 오프라인 발표심사 (PT 15분 + QA 5분) | 2026-10-13 |
| 최종 결과 발표 | 2026-10-23 |

> **오늘(2026-08-08) 기준 MVP까지 약 30일.**

---

## 1. 문제 정의

정부·지자체·공공기관은 매년 수천 건의 지원금·정책자금·금융상품·법령을 쏟아내지만, 정보는 부처별 사이트에 흩어져 있고 자격요건은 "만 19~34세, 중위소득 150% 이하, 무주택, 해당 시군 6개월 이상 거주" 같은 조합형이다. 결과:

- **정보 비대칭**: 받을 수 있는데 몰라서 못 받는다 (미신청 사각지대)
- **탐색 비용**: 내가 대상인지 확인하려면 공고문 PDF를 끝까지 읽어야 한다
- **디지털 격차**: 고령층·저소득층일수록 탐색 능력이 낮은데 지원 필요성은 가장 높다

### 해결 가설

관련 정보는 두 조건을 **동시에** 만족해야 한다.

| 축 | 질문 | 입력 | 판정 |
|---|---|---|---|
| **자격** | 내가 대상인가? | 인적사항 + 소득 두 기준 | 정형 조건 대조 (SQL) |
| **의도** | 내가 찾는 것인가? | 원하는 것 한 줄 | 의미 유사도 (벡터) |

자격만 맞으면 수백 건의 무관한 정보가 쏟아지고, 의도만 맞으면 대상도 아닌 것을 권하게 된다. 두 축이 **서로 다른 것을 재기 때문에** 교집합이 정보가 된다.

### 차별점 (심사 대응)

1. **직교하는 두 축의 교차 검증.** 자격 판정(규칙)과 의도 매칭(벡터)이 서로 다른 실패 모드를 막는다 — 자격만 맞는 정보 홍수와, 그럴듯하지만 대상이 아닌 추천.
2. **파싱이 생성형 모델에 의존하지 않는다.** 자격요건 추출은 정규식과 사전으로 결정적으로 처리하고, 추출 근거(매칭된 원문 구간)를 보관해 오탐 시 원인을 특정할 수 있다.
3. **모든 카드에 원문 출처 URL과 수집 시각을 명시한다.**

---

## 2. 사용자 & 시나리오

### 페르소나

| # | 인적사항 | 원하는 것 (입력) | 기대 결과 |
|---|---|---|---|
| P1 | 28세 / 여 / 사무직 / 서울 관악구 / 3분위 | "보증금 올려달래서 대출 알아봐요" | 중소기업 청년 전월세보증금 대출, 버팀목 전세자금, 청년월세지원 |
| P2 | 41세 / 남 / 자영업(요식업) / 부산 해운대구 / 5분위 | "가게 확장하려는데 자금이 필요해요" | 소상공인 정책자금, 지역신보 보증부대출, 노란우산공제 |
| P3 | 67세 / 여 / 무직 / 전남 순천시 / 1분위 | *(입력 없이 건너뜀)* | 자격 통과분 전체를 점수순 — 기초연금, 긴급복지지원, 농어촌 주거개선 융자 … |

> P3처럼 건너뛰는 사용자가 상당수일 것으로 본다. 이 경우 의도 축 없이 자격 축만으로 동작한다 (§7.3).

### 핵심 시나리오

1. 온보딩 6단계 입력 (2분 이내, 로그인 불필요)
2. 결과 리스트: 관련도 정렬 카드 — 제목 / 한 줄 요약 / 마감일(D-n) / 매칭 근거 배지 (지원금액은 상세 화면에서 표시)
3. 카드 클릭 → 상세: 자격요건 체크리스트(내 프로필 대조 ✅/❌), 신청 방법, 원문 링크

---

## 3. 데이터 소스

### 3.1 원칙: 공식 Open API 우선, 크롤링은 보완

대상 기관 상당수가 이미 공공데이터포털에 Open API를 제공한다. 크롤링은 구조 변경에 취약하고, 심사 구간(9/7~9/11)에 차단당하면 결격 위험이 있다. **API가 있으면 API, 없을 때만 크롤링.**

### 3.2 수집 주기 (전 소스 공통)

- **1일 1회, 심야 배치.** T1~T3 모두 동일. GitHub Actions cron
- 사용자 요청은 DB 조회와 질의 임베딩을 수행하며, 질의가 있는 최초 전체 검색에 한해 OpenRouter 답변을 추가한다. 최초 응답은 최대 12초 지연될 수 있지만 답변 실패가 카드 결과를 없애지는 않는다
- **증분 수집**: 소스가 수정일·등록일 파라미터를 지원하면 전일 이후 변경분만 조회한다. 지원하지 않는 소스는 `content_hash` 비교로 변경분만 재파싱·재임베딩한다
- **일일 호출 한도는 소스별로 100배 차이가 난다** (§3.3 표). 특히 **중앙부처복지서비스 개발계정은 상세조회 100회/일**로, 건당 1회를 쓰면 하루 100건이 상한이다 — 실측 461건 기준 초기 전량 적재에 5~6일이 걸린다. **운영계정은 신청하지 않는다** (활용사례 등록과 심사가 선행되어 대회 일정에 맞지 않는다). 개발계정 한도 안에서 **회차를 나눠 누적**하는 것으로 대신하며, 수집기가 세 가지를 함께 지킨다:
  - **이미 적재된 `external_id`는 상세를 건너뛴다.** 그 회차의 한도를 신규 건에 몰아준다
  - **회차당 상세 호출을 `maxDetailCalls`(기본 90)에서 끊는다.** 목록 호출분과 재시도 여유를 남긴다
  - **한도를 넘겨 429가 나면 예외를 던지지 않고 그때까지 모은 건을 반환한다.** 이것이 없으면 회차 전체가 롤백되어 매일 처음부터 다시 시작하고, 초기 적재가 영영 완료되지 않는다
  - 대가: 적재된 건을 다시 조회하지 않으므로 **원본 수정(마감 연장·자격 완화)을 놓친다.** 한도가 풀리면 스킵을 제거해 `content_hash` 비교로 되돌린다
  - 커버 영역이 겹치면서 한도가 100배 넉넉한 **행안부 보조금24(10,000회/일)**를 초기 적재의 주력으로 삼는다
- 실패 시 직전 성공 스냅샷 유지 (빈 결과 노출 금지)
- **콜드 스타트는 이 정책의 사각지대다.** 첫 배포에는 지켜줄 직전 스냅샷이 없고, 상세조회 100회/일 소스는 전량 적재에 6일이 걸린다. 세 가지로 대응한다:
  - `web/scripts/coldstart-eta.mjs` — 소스별 적재 건수와 완료까지 남은 일수를 계산한다 (`npm run coldstart:eta`)
  - **적재 0건일 때 빈 화면 대신 상태 안내를 띄운다.** "내 조건에 맞는 게 없다"와 "아직 데이터가 없다"는 사용자에게 전혀 다른 사실인데, 둘 다 빈 결과로 보이면 서비스가 고장 난 것으로 읽힌다 (`MatchResponse.catalogEmpty`)
  - **시드 스냅샷**: 적재가 한 번 완주하면 `pg_dump` 로 덤프해 둔다. DB 가 날아가도 6일을 다시 기다리지 않는다. 절차는 [db/README.md §10](db/README.md)
- **주 1회 전량 대조 (일요일).** 증분 수집만으로는 원본에서 조용히 내려간 공고를 잡지 못한다. 마감일이 있으면 `ends_at`으로 자동 만료되지만, 상시 공고(`is_always_open`)가 원본에서 사라지면 DB에 영원히 남아 죽은 링크를 계속 추천하게 된다. 전량 목록을 받아 우리 DB에만 있는 건을 `expired` 처리한다. 이때는 **ID 목록만** 대조하므로 상세 조회 호출이 들지 않는다

#### 신규 / 수정 / 무변경 판정

공고는 새로 올라오기도 하고 기존 건이 수정되기도 한다(마감 연장, 예산 증액, 자격 완화). 둘을 구분하려면 **소스 쪽 고유 식별자**가 필요하다.

```
external_id = source_key + ':' + 원본 공고 고유번호      -- UNIQUE
```

`source_url`을 키로 쓰지 않는다 — 쿼리 파라미터나 세션 토큰이 붙어 같은 공고가 매번 다른 URL로 보일 수 있다. 원본 고유번호를 주지 않는 T3 크롤링 소스는 `sha1(issuer + title)`로 대체한다.

매 건마다 `external_id` 조회 후 `content_hash`를 비교해 분기한다.

`content_hash`는 파서 버전 접두사와 **정규화한 비교 대상 필드만**(제목·본문·기간·금액 등)으로 계산한다. API 응답에는 조회수·응답 생성 시각처럼 매 호출 바뀌는 필드가 섞여 있는 경우가 많은데, 이를 해시에 포함하면 전 건이 매일 "수정"으로 판정돼 재파싱·재임베딩이 전량 발생한다 — 증분 수집의 비용 이점이 통째로 사라지는 조용한 실패 모드다. 반대로 파서 의미가 바뀌면 접두사 버전을 올려 원문이 같은 기존 공고도 다음 수집에서 한 번 재파싱한다. 상세 호출 한도가 있는 소스의 `knownIds`는 현재 파서 버전으로 처리된 ID만 건너뛰므로, 재파싱도 호출 한도 안에서 여러 회차에 걸쳐 이어진다.

| 상황 | 판정 | 처리 |
|---|---|---|
| `external_id` 없음 | **신규** | `raw_documents` insert → 파싱 → 임베딩 → `programs` insert |
| 있음 + hash 동일 | **무변경** | `fetched_at`만 갱신. 파싱·임베딩 호출 없음 |
| 있음 + hash 상이 | **수정** | `raw_documents` **insert**(덮어쓰지 않음 → 버전 이력) → 재파싱 → 재임베딩 → `programs` **UPDATE** |
| 전량 대조에서 원본에 없음 | **삭제** | `status = 'expired'`. 행은 지우지 않는다. 단 `program_embeddings`의 해당 행은 삭제 — 만료 건이 벡터 top-k 슬롯을 차지하면 유효 결과가 밀려난다 (§7.3) |

수정 시 `programs.id`를 **유지한다.** 새로 만들면 이미 공유된 상세 페이지 URL이 깨진다.

원문 이력이 `raw_documents`에 누적되므로 별도 버전 테이블이 필요 없다. "언제 무엇이 바뀌었나"는 같은 `external_id`의 행을 `fetched_at` 순으로 보면 된다.

### 3.3 소스 목록

> **API 존재 여부·신청 창구·트래픽 한도와 공개 응답 명세는 2026-08-10 공식 문서로 재확인했다.** 보조금24·중앙부처복지·지자체복지·K-Startup은 공개 Swagger를 기준으로 전용 어댑터를 둔다. 명세만으로 확정할 수 없는 반복 노드 형태와 실제 값 표기는 키 발급 후 성공 응답 fixture로 검증한다 (§12 W1).

| 티어 | 소스 | 신청 창구 | 개발계정 한도 | 커버 영역 |
|---|---|---|---|---|
| **T1** | 행안부 **대한민국 공공서비스(혜택) 정보** (보조금24) | data.go.kr `15113968` | **10,000/일** | 중앙부처+지자체+공공기관+교육청 혜택 전체 |
| **T1** | 사회보장정보원 **중앙부처복지서비스** | data.go.kr `15090532` | **100/일** ⚠️ | 중앙부처 복지·지원금 |
| **T1** | 사회보장정보원 **지자체복지서비스** | data.go.kr `15108347` | 1,000/일 | 지자체 복지·지원금 |
| **T1** | **기업마당** 지원사업정보 | bizinfo.go.kr 자체 | 미공개 (키 유효 60분 명시) | 소상공인·창업·중소기업 정책자금 |
| **T1** | 금감원 **금융상품통합비교공시** (구현 6종) | finlife.fss.or.kr 자체 | 미공개 | 예·적금, 담보/전세/개인신용대출, 연금저축. 운영 키로 상품·권역 실응답 검증 필요 |
| **T1-인프라** | 행안부 **행정표준코드(법정동코드)** | data.go.kr `15077871` / code.go.kr | — | §5 `regions`·§6.1 지역 사전의 입력. **API 연동 아님 — 1회 다운로드 후 정적 파일** |
| **T2** | 한국고용정보원 **온통청년 청년정책** | youthcenter.go.kr (심사 승인제) / data.go.kr `15143273` | 심사 후 결정 | 청년정책 전용. **대회 세부주제 직결 (§0)** |
| T2 | 창업진흥원 **K-Startup 사업공고** | data.go.kr `15125364` | 자동승인 | 창업지원사업 (P2 페르소나 보강) |
| T2 | 한국부동산원 **청약홈 분양정보** | data.go.kr `15098547` | 자동승인 | 주택 분양 공고 |
| T2 | **법제처 국가법령정보** | open.law.go.kr (OC 발급) | — | 관련 법령·시행령 원문 |
| T2 | **서울 열린데이터광장** 등 광역 포털 | data.seoul.go.kr | 1,000/일 (1회 1,000건) | 지자체 자체 사업 |
| **T3** | **주택도시기금** (디딤돌·버팀목 등) | — (API 없음) | — | P1 페르소나의 전세자금대출. T1/T2 누락분 |

**공식 명세·예시 링크 (2026-08-10 확인):**

- 보조금24: [data.go.kr `15113968`](https://www.data.go.kr/data/15113968/openapi.do) / [공개 Swagger JSON](https://infuser.odcloud.kr/api/stages/44436/api-docs)
- 중앙부처복지: [data.go.kr `15090532`](https://www.data.go.kr/data/15090532/openapi.do)
- 지자체복지: [data.go.kr `15108347`](https://www.data.go.kr/data/15108347/openapi.do) — 페이지 내 Swagger와 코드표 제공
- K-Startup: [data.go.kr `15125364`](https://www.data.go.kr/data/15125364/openapi.do) — 페이지 내 Swagger와 공식 가이드 ZIP 제공
- 온통청년: [data.go.kr `15143273`](https://www.data.go.kr/data/15143273/openapi.do) / [API 문서](https://www.youthcenter.go.kr/cmnFooter/openapiIntro/oaiDoc) / [이용방법](https://www.youthcenter.go.kr/cmnFooter/openapiIntro/oaiGuide)
- 청약홈: [data.go.kr `15098547`](https://www.data.go.kr/data/15098547/openapi.do) / [한국부동산원 기술문서](https://www.reb.or.kr/reb/na/ntt/selectNttInfo.do?mi=10251&bbsId=1268&nttSn=79889)
- 국가법령정보: [data.go.kr `15000115`](https://www.data.go.kr/data/15000115/openapi.do) / [법제처 OpenAPI 활용가이드](https://open.law.go.kr/LSO/openApi/guideList.do)
- 서울 열린데이터광장: [OpenAPI 이용방법·샘플](https://data.seoul.go.kr/together/guide/useGuide.do) / [인증키 발급](https://data.seoul.go.kr/together/mypage/actKey.do)

전용 어댑터는 API의 인증·페이지·응답 봉투·필드 매핑만 담당한다. 서술형 `지원대상`·`선정기준`은 공통 자격 파서로 넘기고, 구조화 조건을 별도 호출할 때는 어댑터에서 정규화해 우선 사용한다. 온통청년은 성공 응답 fixture 확보 뒤, 청약홈은 공고문 원문 확보 경로가 정해진 뒤 추가한다. 법제처는 지원사업 수집기가 아니라 근거 법령 보강용이며, 서울은 구체적인 정책 데이터셋을 먼저 선정해야 한다.

현재 전용 어댑터는 `gov24`, `local_welfare`, `social_security`, `bizinfo`, `finlife`, `kstartup` 6종을 구현했고, 배치 기본 실행은 `kstartup`을 뺀 T1 5종이다. 공식 명세를 재현한 오프라인 fixture는 기본 테스트에 포함한다. T1인 `gov24`와 `local_welfare`는 운영 스케줄 기본 소스이며 배포 전에 데이터셋별 활용승인과 첫 성공 응답 대조가 필수다. T2인 `kstartup`은 승인 후 `--source`로 연동한다. 보조금24는 일괄 목록이 이미 지원대상·선정기준을 제공하므로 현재 배치는 페이지당 1회만 호출하며, 건별 `supportConditions` 조회는 호출량과 실효성을 표본 검증한 뒤 추가한다.

**확정된 엔드포인트 (문서 공개분):**

```
기업마당    GET https://www.bizinfo.go.kr/uss/rss/bizinfoApi.do
            crtfcKey(필수) dataType=json|rss searchLclasId hashtags pageUnit pageIndex
            → 공고명 / 공고URL / 공고ID / 소관기관명 / 사업개요 / 지원분야 / 등록일자 / 신청기간 / 지원대상
            ※ 공고ID가 있으므로 external_id를 sha1 대체 없이 그대로 쓴다 (§3.2)

보조금24    GET https://api.odcloud.kr/api/gov24/v3/serviceList
                 https://api.odcloud.kr/api/gov24/v3/serviceDetail
                 https://api.odcloud.kr/api/gov24/v3/supportConditions
            serviceKey page perPage returnType cond[수정일시::GTE] cond[서비스ID::EQ]
            → JSON { page, perPage, totalCount, currentCount, matchCount, data[] }
            → supportConditions: 연령·중위소득·성별·가구·직업·장애 등 구조화 조건

지자체복지  GET https://apis.data.go.kr/B554287/LocalGovernmentWelfareInformations/LcgvWelfarelist
                 https://apis.data.go.kr/B554287/LocalGovernmentWelfareInformations/LcgvWelfaredetailed
            serviceKey pageNo numOfRows / 상세 servId
            → XML 목록 servList, 상세 sprtTrgtCn·slctCritCn·alwServCn·aplyMtdCn

K-Startup   GET https://apis.data.go.kr/B552735/kisedKstartupService01/getAnnouncementInformation01
            serviceKey page perPage returnType=json
            → 공고ID·지원대상·제외대상·연령·창업기간·지역·우대사항·접수기간

행정표준코드 GET http://apis.data.go.kr/1741000/StanReginCd/getStanReginCdList
온통청년     GET https://www.youthcenter.go.kr/opi/youthPlcyList.do
            openApiVlak pageIndex display query bizTycdSel srchPolyBizSecd
            ※ 2023-06-14 필드 개편 — "청년정책(신)" 탭 기준으로 연동할 것

금감원       GET https://finlife.fss.or.kr/finlifeapi/{상품}.json   (.xml 도 문서화됨)
            auth(필수) topFinGrpNo(금융권역) financeCd pageNo
            {상품} = depositProductsSearch      예금
                     savingProductsSearch       적금
                     annuitySavingProductsSearch 연금저축
                     mortgageLoanProductsSearch  주택담보대출
                     rentHouseLoanProductsSearch 전세자금대출
                     creditLoanProductsSearch    개인신용대출
            topFinGrpNo = 020000 은행 / 030200 여신전문 / 030300 저축은행
                          050000 보험 / 060000 금융투자
            ※ 위 상품 경로와 권역 코드는 2차 자료 기반 — 키 발급 후 실호출로 검증할 것
            ※ P1 페르소나(전세보증금 인상)에는 rentHouseLoanProductsSearch 가 필수다
```

**소스 지위 (대회 규정 대응):** 전 소스가 중앙행정기관·지자체·준정부기관이며 민간 소스는 없다. 단 **금융감독원은 무자본 특수법인으로 공공기관운영법상 공공기관은 아니다** — 기획서에는 "전 소스 공공기관"이 아니라 **"정부·공공기관 공식 데이터"**로 기재한다.

**라이선스:** data.go.kr 경유 소스는 "이용허락범위 제한 없음"이 명시돼 있다. **포털 밖에서 직접 신청하는 3종(기업마당·금감원·온통청년)은 각자 이용약관을 따르므로 키 발급 시 재배포·가공 조건을 확인**하고 그 결과를 부록 체크리스트의 외부 데이터 공개 항목에 반영한다.

**신청 창구가 갈린다.** MVP는 data.go.kr / bizinfo / finlife를 사용한다. T2 확장 시 온통청년 자체 API와 서울 열린데이터광장을 별도 신청하고, 법제처 직접 API는 근거 법령 보강을 실제로 구현할 때만 신청한다. 순차 진행하면 W1을 넘기므로 필요한 창구는 **Day 1에 병렬 신청한다.**

**W1에서 결정할 것 — 온통청년의 T1 승격 여부.** 대회 세부주제가 "청년층 자산 형성"(§0)이고, 청년정책은 등록 단계에서 연령·소득·취업상태를 코드로 받는 구조라 **자격요건이 정형 필드로 올 가능성이 가장 높은 소스**다. 실호출로 확인되면 §12-1 리스크의 헤지가 되므로 T1으로 올리고, 서술형이면 T2에 둔다.

### 3.4 크롤링 정책 (T3에만 추가 적용)

- `robots.txt` 준수, User-Agent에 서비스명·연락처 명시
- 도메인당 1 req/sec, 동시 1커넥션
- 공개 공고문만 수집. 로그인·개인정보 페이지 접근 없음

### 3.5 인증키 발급 계획

**키 개수와 신청 건수는 다르다.** 공공데이터포털은 계정당 인증키 1개를 발급하고, 그 키로 쓸 API를 **데이터셋별로 따로 활용신청**한다 (대부분 자동승인, 즉시 사용). 즉 data.go.kr 소스가 몇 개든 키는 1개다.

| # | 키 | 발급처 | 활용신청 대상 | 건수 |
|---|---|---|---|---|
| 1 | `DATA_GO_KR_API_KEY` | [data.go.kr](https://www.data.go.kr/) (자동승인) | 보조금24 [`15113968`](https://www.data.go.kr/data/15113968/openapi.do) / 중앙부처복지 [`15090532`](https://www.data.go.kr/data/15090532/openapi.do) / 지자체복지 [`15108347`](https://www.data.go.kr/data/15108347/openapi.do) / K-Startup [`15125364`](https://www.data.go.kr/data/15125364/openapi.do) / 청약홈 [`15098547`](https://www.data.go.kr/data/15098547/openapi.do) | **5** |
| 2 | `BIZINFO_API_KEY` | [bizinfo.go.kr](https://www.bizinfo.go.kr/apiDetail.do?id=bizinfoApi) (상세 페이지 하단 폼) | 지원사업정보 | 1 |
| 3 | `FINLIFE_API_KEY` | [finlife.fss.or.kr](https://finlife.fss.or.kr/finlife/main/contents.do?menuNo=700029) | 코드상 금융상품 6종 — 운영 키로 지원 범위 검증 필요 | 1 |
| 4 | `YOUTHCENTER_API_KEY` | [온통청년](https://www.youthcenter.go.kr/cmnFooter/openapiIntro/oaiGuide) (심사 승인) | 청년정책 | 1 |
| 5 | `LAW_OPEN_API_OC` | [법제처](https://open.law.go.kr/LSO/openApi/guideList.do) | 국가법령정보 | 1 |
| 6 | `SEOUL_OPENAPI_KEY` | [data.seoul.go.kr](https://data.seoul.go.kr/together/mypage/actKey.do) (가입 즉시) | 서울 열린데이터광장 | 1 |
| 7 | `EMBEDDING_API_KEY` | [Voyage](https://dashboard.voyageai.com/organization/api-keys) / [OpenAI](https://platform.openai.com/api-keys) (§4 W1 확정) | — | 1 |
| 8 | `OPENROUTER_API_KEY` | [OpenRouter](https://openrouter.ai/settings/keys) | — | 1 |
| | | | **합계** | **키 8 / 신청 12** |

**MVP(§11 범위)만이면 키 5 / 신청 7이다** — data.go.kr 3건(보조금24·중앙부처·지자체) + 기업마당 + 금감원 + 임베딩 + OpenRouter. 나머지는 T2 확장분이다.

**링크 검증 상태 (2026-08-10 조회).** 위 data.go.kr 7개 데이터셋, 보조금24 공개 Swagger, 온통청년 문서, 청약홈 기술문서, 법제처 가이드, 서울 OpenAPI 가이드를 개별 확인했다. 공개 명세는 어댑터 구현 근거로 사용하되, 실제 성공 응답이 없는 소스는 키 발급 후 fixture를 확보하기 전까지 운영 검증 완료로 보지 않는다.

**data.go.kr 서비스키는 Decoding / Encoding 두 형태로 발급된다.** 수집기는 `URLSearchParams`로 넘기므로 **Decoding 키**를 넣어 한 번만 인코딩하게 한다. URL에 직접 문자열로 붙일 때만 Encoding 키다. 이중 인코딩(`%`→`%25`)과 디코딩 키의 `+`가 공백으로 바뀌는 것이 인증 실패의 대표 원인이다 ([게이트웨이 가이드](https://www.data.go.kr/images/biz/swagger-guide/gw/gateway_swagger_guide.pdf)).

**중복 신청·추가 신청 주의:**

- **행정표준코드 — 키 불필요.** code.go.kr 전체 다운로드로 CSV 1회 받아 정적 파일로 둔다. API로 붙일 이유가 없다 (연 단위로만 바뀐다)
- **온통청년 — 자체 키가 필요하다.** data.go.kr `15143273`은 LINK형 카탈로그이며, 성공 fixture는 youthcenter 로그인·API 신청·심사 승인 후 확보한다
- **법제처 — 구현할 때 OC를 별도 발급한다.** data.go.kr `15000115`는 카탈로그 링크로만 사용하고, 실제 `lawService.do` 호출은 법제처 OC를 사용한다
- **Supabase / Vercel** — 신청이 아니라 프로젝트 생성 시 자동 발급 (`SUPABASE_URL`, `SERVICE_ROLE_KEY` 등). 위 6종에 포함하지 않음

**추가 신청은 없다.** 중앙부처복지서비스 운영계정 전환은 활용사례 등록과 심사가 선행되어 대회 일정에 맞지 않으므로 신청하지 않는다. 개발계정 100회/일 안에서 회차를 나눠 누적 적재한다 (§3.2).

---

## 4. 시스템 구조

```
[배치 · 1일 1회]

  API 호출 / T3 크롤링
        ↓
  raw_documents  (원문 보관, 덮어쓰지 않음)
        ↓
    ┌───┴──────────────┬───────────────────┐
    ↓                  ↓                   ↓
 ① 자격요건 파싱     ② 임베딩            ③ 요약·절차 생성
   Regex·사전          본문 청크 → 벡터     결정형 문구, 신규·수정 시 1회
    ↓                  ↓                   ↓
 eligibility_rules  program_embeddings  programs.summary
                     (pgvector)          programs.apply_steps
    └──────────────────┴───────────────────┘
                 ↓
           Postgres (단일 DB)

[사용자 요청]

  인적사항·소득 기준 ─→ (A) SQL 자격 필터 → 집합 A  "대상이 되는 것"
  원하는 것 한 줄 ─→ (B) 임베딩 → top-k  → 집합 B  "찾는 것과 가까운 것"
                 ↓
           A ∩ B  (입력 있을 때)
           A      (건너뛴 경우)
                 ↓
           스코어링 정렬
                 ↓
           카드 렌더 — 요약·절차와 매칭 근거는 결정형 문구
                 ↓
           질의가 있는 최초 전체 검색만 상위 5건 공개 메타데이터 기반 OpenRouter 답변
                 ↓
              결과 화면
```

### 기술 선택

| 레이어 | 선택 | 이유 |
|---|---|---|
| 프론트+API | Next.js (App Router) + TypeScript / Vercel | 한 저장소, 배포 URL 즉시 확보 |
| SQL DB + 벡터 DB | **Supabase Postgres + `pgvector`**, `postgres`(postgres.js) | 별도 벡터 DB 불필요. 교집합을 한 쿼리로 처리 |
| 수집·파싱 | 독립 TypeScript Node 배치 (`web/ingest`) + native `fetch` | 웹과 언어·의존성을 공유하고 GitHub Actions cron 실행 |
| 임베딩 | Voyage AI `voyage-4-large` (1024차원) | 문서 `document` / 질의 `query` 입력 타입 분리, W1 한국어 성능 실측 |
| LLM | OpenRouter (`google/gemma-4-31b-it:free`) | 질의가 있는 최초 전체 검색의 상위 5건 공개 메타데이터를 바탕으로 답변 하나 생성. 12초·1회로 제한하며 실패해도 카드 결과는 유지 (§7.5) |
| 인증 | 없음 (익명 세션). 프로필은 **암호화한** httpOnly 쿠키에만 | 개인정보 최소화 — 서버 저장 없음 (§8) |

**벡터 DB를 따로 두지 않는 이유:** 교집합 연산이 이 설계의 핵심인데, DB가 둘로 나뉘면 A와 B를 애플리케이션 메모리로 끌어와 맞춰야 한다. pgvector면 한 쿼리로 끝난다. 데이터 규모(수천~수만 건)도 pgvector 범위 안이다.

---

## 5. 데이터 모델

```sql
-- 수집 원문 (append-only. 수정분도 새 행으로 쌓아 버전 이력이 된다)
raw_documents (
  id,
  external_id,                     -- 'source_key:원본고유번호' — 동일성 판정 키
  source_key, source_url, fetched_at,
  content_hash, raw_body
)
CREATE INDEX ON raw_documents (external_id, fetched_at DESC);

-- 정규화된 프로그램 (최신 상태만. 수정 시 UPDATE, id 유지)
programs (
  id,
  external_id UNIQUE,              -- upsert 대상 키
  raw_document_id,                 -- 현재 반영된 원문 버전
  first_seen_at, last_changed_at,
  title, body_text,                -- 임베딩 입력 원본
  summary,                         -- 배치의 결정형 문구 (§7.5)
  apply_steps jsonb,               -- 신청 절차 3단계, 배치의 결정형 문구 (§7.5)
  form,                            -- subsidy | loan | tax | product | law
  issuer, issuer_level,            -- central | metro | local
  benefit_amount_text, benefit_amount_min, benefit_amount_max,
  apply_url, apply_method,
  starts_at, ends_at, is_always_open,
  source_url, fetched_at,
  status                           -- active | expired
                                   -- 자동 추출이 불완전한 건도 active 로 둔다. 숨기지 않는 이유는 §6.2
)

-- ① 정형 자격요건
eligibility_rules (
  program_id PRIMARY KEY,          -- 프로그램당 정확히 1행 — §7.3 eligible의 JOIN이 중복 행을 만들지 않는 전제
  age_min, age_max,                -- NULL = 무관
  gender,                          -- NULL | 'M' | 'F'
  regions text[],                  -- 행정구역 코드. NULL = 전국. 시도 2자리 / 시군구 5자리로 통일 저장
                                   -- 질의 시 :region_prefixes = [시도 2자리, 시군구 5자리] 두 값.
                                   -- `&&`는 원소 '동등' 비교이므로 양쪽이 같은 자리수 체계를 써야만 매칭된다
  occupations text[],              -- NULL = 무관
  income_decile_max,               -- 소득분위 상한. NULL = 무관
  median_income_percent_max,       -- 기준중위소득 비율 상한. NULL = 무관
  extra_conditions jsonb,          -- 무주택 등 정형화 실패분 (표시 전용)
  parse_method,                    -- regex | llm | mixed
  parse_evidence jsonb,            -- 필드별 매칭된 원문 구간 (추적용)
  confidence real
)

-- ② 벡터 인덱스 (긴 본문은 청크별 행. 질의 시 프로그램 단위 MAX(sim) 집계 — §7.1)
program_embeddings (
  program_id,
  chunk_idx int,                   -- 짧은 문서는 0 하나
  embedding vector(1024),          -- 차원은 모델 확정 후
  provider text,                   -- 벡터 공간: provider[:model] (voyage:voyage-4-large). 변경 시 재색인
  embedded_at,
  PRIMARY KEY (program_id, chunk_idx)
)
CREATE INDEX ON program_embeddings USING hnsw (embedding vector_cosine_ops);
-- 재임베딩 시 해당 program_id 청크 전체 삭제 후 재삽입 (청크 수가 줄어도 잔여 행이 안 남게)
-- status='expired' 전환 시 임베딩 행 삭제 — 만료 건이 벡터 top-k 슬롯을 차지하지 않게 한다

-- 사용자 프로필 테이블은 없다.
-- 나이·성별·직업·지역·소득분위·기준중위소득 비율은 AES-256-GCM 으로 암호화한
-- httpOnly 쿠키에만 담고 서버에 저장하지 않는다 (§8). 매칭은 요청 시점에 쿠키를 읽어 처리한다.
```

`form`은 결과 화면의 탭(지원금 / 대출 / 금융상품 / 법령)에만 쓴다. 검색 전 필터가 아니라 **결과를 본 뒤 좁히는 용도**다.

> **주제 분류 컬럼을 두지 않는 이유:** 사용자가 원하는 것을 문장으로 직접 입력하므로, 그것을 6~10개 카테고리로 미리 접어둘 이유가 없다. 주제 사전은 유지 비용이 들고, 사전이 놓친 프로그램이 그대로 노출 누락으로 이어진다. "전세 vs 월세 vs 보증금 인상"의 차이는 카테고리가 아니라 임베딩이 구분한다.

### 입력 필드 정의

| # | 필드 | 입력 방식 | 쓰이는 곳 |
|---|---|---|---|
| 1 | 나이 | 숫자 (만 나이) | SQL |
| 2 | 성별 | 남 / 여 / 선택 안 함 | SQL (미선택 시 성별 조건 프로그램도 포함) |
| 3 | 직업·직무 | 대분류 12종 택1 → 소분류 | SQL |
| 4 | 거주지역 | 시/도 → 시군구 2단계 (행정표준코드) | SQL |
| 5 | 소득 | 소득분위(1~10 또는 모름) + 가구원 수·월소득 선택 계산 | SQL |
| 6 | **원하는 것** | **자유입력 한 줄 (200자), 건너뛰기 가능** | **벡터** |

**소득** — 사용자가 아는 소득분위는 그대로 입력한다. 별도로 가구원 수와 월 가구소득(또는 알고 있는 소득인정액)을 입력하면 브라우저가 보건복지부의 2026년 가구원별 기준중위소득표로 비율을 계산한다. 두 축은 서로 환산하지 않고 공고문과 같은 단위끼리만 비교한다. 실제 월 금액과 가구원 수는 서버로 보내거나 저장하지 않고 계산된 정수 비율만 저장한다. 월소득만 쓴 값은 재산의 소득환산액 등이 빠진 예상치이므로 상세 화면에서 원문 확인을 안내한다. 기준표는 `shared/median_income_2026.json`으로 두고 연 1회 갱신한다.

**원하는 것** — 의도 축의 유일한 입력이다.

```
어떤 도움이 필요하신가요?
┌────────────────────────────────────────┐
│                                        │
└────────────────────────────────────────┘

이런 걸 찾으시나요?  (누르면 입력됩니다)
 · 보증금 올려달래서 대출 알아봐요
 · 가게 확장하려는데 자금이 필요해요
 · 목돈 모으고 싶어요
 · 병원비가 부담돼요

              [ 건너뛰기 ]
```

예시 문구는 **필터가 아니라 입력 도우미다.** 누르면 텍스트가 입력칸에 채워지고 사용자가 그대로 쓰거나 고칠 수 있다. "뭘 찾을지 모르겠는" 사용자와 타이핑이 부담스러운 고령층 문제를 백엔드 비용 없이 해결한다.

건너뛰면 의도 축 없이 자격 축만으로 결과를 낸다 (§7.3).

---

## 6. ① 자격요건 파싱 (Regex 우선)

공고문 자격요건 문구는 표현이 상당히 정형화되어 있어 정규식으로 다수를 잡을 수 있다. LLM 호출 없이 처리되므로 빠르고, 공짜고, **결과가 항상 같다.**

### 6.1 패턴

> **아래는 형태를 보여주는 예시이며 확정 스펙이 아니다.** 실제 패턴은 API 응답을 받아본 뒤 원문을 보면서 정리한다. 미리 짜봐야 실제 표기와 어긋나 버리는 작업이다.

패턴 정리는 눈으로 훑지 말고 **미매칭 리포트로 돌린다** — 파서를 돌린 뒤 필드별 미추출 건수를 세고, 미추출 문서에서 자격요건 문단만 빈도순으로 뽑는다. 상위 표현부터 패턴을 추가하고 다시 돌리는 루프다. 커버리지가 안 오르는 지점의 조건은 `extra_conditions`에 원문 그대로 보존한다.

```ts
const AGE = [
  /만\s*(\d{1,3})\s*세\s*(?:이상|부터)/,
  /만\s*(\d{1,3})\s*세\s*(?:이하|까지)/,
  /만\s*(\d{1,3})\s*세\s*미만/, // 미만은 경계 미포함
  /만?\s*(\d{1,3})\s*[~-]\s*(\d{1,3})\s*세/,
];

const INCOME = [
  /기준\s*중위소득\s*(\d{1,3})\s*%\s*이하/,
  /소득\s*(\d{1,2})\s*분위\s*이하/,
];

const GENDER = [/여성만|여성에\s*한(?:함|정)/];
```

지역·직업은 정규식이 아니라 **사전 룩업**이 맞다. 행정표준코드 사전(시도 17 + 시군구 250여)과 직업 동의어 사전(`소상공인`/`자영업자` → `self_employed`)으로 문자열 매칭한다.

`중위소득 %`는 `median_income_percent_max`, 명시적인 `N분위`는 `income_decile_max`에 저장한다. 차상위·기초생활수급 문구는 두 수치 중 하나로 추정하지 않고 `extra_conditions`에 보존한다.

### 6.2 미추출 조건

정규식·사전으로 구조화할 수 없는 조건은 추정하지 않는다. 해당 필드는 `NULL`로 두고
`extra_conditions`에 원문을 보존하며, 상세 화면에서 "추가 확인 필요 조건"으로 알린다.
`NULL`은 §7.3에서 '조건 없음 = 통과'이므로 자동 추출의 불확실성 때문에 사용자를
잘못 탈락시키지 않는다.

### 6.3 정규식의 한계 (알고 가는 것)

"관내 6개월 이상 거주하며 무주택이고 최근 1년간 유사 지원을 받지 않은 자" 같은 복합 조건은 정형 필드에 넣지 않는다. 이런 항목은 `extra_conditions`에 원문 그대로 넣고 **필터링에 쓰지 않는다.** 대신 상세 화면에 "추가 확인 필요 조건"으로 노출한다. 자동 판정을 포기하는 대신 잘못된 탈락을 만들지 않는다.

---

## 7. ② 벡터 검색 & 교차 검증

### 7.1 색인

프로그램의 `title + summary + body_text`(자격요건 문단 포함)를 임베딩해 저장한다. 본문이 길면 문단 단위로 청킹해 **청크별 행으로 저장**하고, 질의 시 프로그램 단위 **최대 유사도(MAX)로 집계**한다. 벡터 자체를 max/mean-pooling으로 합치지 않는다 — 임베딩 공간에서 좌표를 섞으면 코사인 유사도가 왜곡되고, "본문 중 한 문단이 강하게 일치"하는 신호(우리가 원하는 것)가 나머지 문단에 희석된다.

### 7.2 질의

**사용자가 입력한 문장을 그대로 임베딩한다.** 인적사항은 넣지 않는다 — 나이·지역·소득은 SQL이 이미 정확하게 판정했고, 임베딩에 섞으면 두 축이 같은 것을 재게 되어 교집합이 무의미해진다.

```
입력: "보증금 올려달래서 대출 알아봐요"
  → 임베딩 → 코사인 top-k
```

의도 문장 ↔ 공고문 본문의 유사도는 인적사항 ↔ 공고문보다 신호가 강하다. 어휘가 실제로 겹치기 때문이다(보증금·대출·전세). W1 실측 대상이지만 사전 기대는 긍정적이다.

### 7.3 교차 검증

```sql
WITH eligible AS (          -- 집합 A: 자격
  SELECT p.id FROM programs p JOIN eligibility_rules e ON e.program_id = p.id
  WHERE p.status = 'active'
    AND (p.ends_at IS NULL OR p.ends_at >= now())
    AND (e.age_min   IS NULL OR :age >= e.age_min)
    AND (e.age_max   IS NULL OR :age <= e.age_max)
    AND (e.gender    IS NULL OR :gender IS NULL OR e.gender = :gender)
                             -- :gender NULL = '선택 안 함'. 성별 조건 프로그램도 포함한다 (§5 입력 필드 정의)
    AND (e.regions   IS NULL OR e.regions && :region_prefixes)
    AND (e.occupations IS NULL OR :occupation = ANY(e.occupations))
    AND (e.income_decile_max IS NULL OR :income_decile IS NULL OR :income_decile <= e.income_decile_max)
    AND (e.median_income_percent_max IS NULL OR :median_income_percent IS NULL
         OR :median_income_percent <= e.median_income_percent_max)
),
similar AS (                -- 집합 B: 벡터 top-k (청크 단위 검색)
  SELECT program_id, embedding <=> :qvec AS dist
  FROM program_embeddings ORDER BY embedding <=> :qvec LIMIT 200
),
similar_by_program AS (     -- 프로그램 단위 최대 유사도로 집계 (§7.1)
  SELECT program_id, 1 - MIN(dist) AS sim
  FROM similar GROUP BY program_id
)
SELECT e.id, s.sim
FROM eligible e
JOIN similar_by_program s ON s.program_id = e.id;   -- 입력을 건너뛰면 이 JOIN을 생략
```

> **top-k 선절단 주의**: HNSW top-k는 자격 필터 **이전에** 잘린다 (사후 필터 구조). 자격 통과분이 top-k 안에 적으면 교집합이 실제보다 작게 나온다 — §7.7의 1단계(k 확대)가 이 케이스의 대응이고, 그래도 부족하면 pgvector 0.8+의 `hnsw.iterative_scan` 옵션을 검토한다. 만료 건의 임베딩을 삭제하는 것(§5)도 같은 이유다.

`NULL = 조건 없음 = 통과`. 자격 조건이 명시되지 않은 프로그램은 배제하지 않는다. **누락이 오탐보다 비싸다** — 이 서비스가 푸는 문제가 "받을 수 있는데 몰라서 못 받는 것"이기 때문이다.

**결과 구성:**

| "원하는 것" 입력 | 주 결과 | 근거 |
|---|---|---|
| 있음 | `A ∩ B` | 자격 + 의도 이중 일치 |
| 건너뜀 | `A` | 자격 통과분 전체, 점수순 정렬 |

`B − A`(자격 미달)는 어느 경우에도 노출하지 않는다.

### 7.4 스코어링 (정렬)

```
score = 0.30 · 유사도       (입력 건너뛰면 0, 나머지 가중치 정규화)
      + 0.25 · 조건_구체성  (내 속성 중 몇 개가 매칭에 관여했나)
      + 0.20 · 지역_근접성  (기초 > 광역 > 전국)
      + 0.15 · 금액_규모    (log 정규화)
      + 0.10 · 마감_임박도  (D-7 이내 가산, 상시 중립)
```

가중치 초기값. 페르소나 3종 골든셋으로 튜닝한다 (§10). 입력을 건너뛴 경로에서는 **마감 임박과 금액이 사실상 정렬을 주도**하므로 이 두 가중치는 별도 확인이 필요하다.

### 7.5 카드 설명과 실시간 답변

카드 문구는 수집 배치가 결정적으로 만들고, 프로필에 의존하는 매칭 근거는 템플릿으로 조립한다. 질의가 있는 최초 전체 검색의 상위 5건에는 별도로 실시간 답변 하나를 붙인다.

| 항목 | 생성 시점 | 방식 |
|---|---|---|
| 한 줄 요약 (40자 이내) | 배치 — 신규·수정 시 1회 | 결정형 문구 → `programs.summary` |
| 신청 절차 3단계 | 배치 — 신규·수정 시 1회 | 결정형 문구 → `programs.apply_steps` |
| 매칭 근거 문장 | 요청 시 | **템플릿.** `eligibility_rules`의 매칭된 필드로 조립 ("만 28세 · 서울 거주 · 3분위 조건에 해당합니다") — 규칙 매칭 결과가 이미 구조화돼 있어 LLM이 필요 없다 |
| 전체 결과 답변 | 질의가 있는 최초 전체 검색(`form=all`, 첫 페이지) | OpenRouter 1회. 질의와 상위 5건의 제목·기관·요약·혜택·마감·원문 URL만 전송 |

OpenRouter에는 프로필·쿠키·공고 원문 본문을 보내지 않고, 답변은 저장하지 않는다. 탭·페이지 이동에서는 답변을 재사용한다. 호출은 12초 1회로 제한하며, 실패하면 답변만 "일시적으로 만들 수 없음"으로 표시하고 카드 결과는 그대로 렌더한다.

**질의 임베딩은 캐시한다.** 같은 문장은 같은 벡터를 만드는데 검색 1회가 곧 과금 1회다 — 특히 온보딩의 예시 문구 4종은 누르면 그대로 전송되므로 사용자 수만큼 같은 문장이 반복된다. 정규화한 질의를 키로 최근 500건을 LRU 로 들고 있으며, **키에 provider·모델명을 포함한다.** 모델을 바꿨는데 옛 벡터를 재사용하면 벡터 공간이 달라 유사도가 조용히 무의미해지기 때문이다.

### 7.6 근접 탈락 (Near-miss)

자격 조건 1개만 어긋난 프로그램을 최대 5건 별도 노출:
> "소득 2분위 이하면 대상입니다 (현재 3분위)"

구현: §7.3의 6개 조건을 각각 `CASE WHEN ... THEN 0 ELSE 1 END`로 위반 수를 합산해 **합계 = 1**인 행을 뽑는다. A 계산과 같은 스캔에서 나오므로 별도 인덱스나 추가 질의가 필요 없다.

### 7.7 빈 결과 처리

교집합이므로 결과 0건이 나올 수 있다. 단계적으로 완화하되 **무엇을 풀었는지 화면에 명시한다.**

1. 벡터 top-k 확대 (200 → 500)
2. 의도 필터 해제 → "찾으시는 것과 딱 맞는 건 없지만, 대상이 되는 지원을 보여드립니다"
3. 그래도 0건 → 근접 탈락(§7.6)만 노출

---

## 8. 비기능 요구사항

### 개인정보 (심사 배점 요소 — 주최가 금융보안원)

- **수집 최소화**: 이름·전화번호·주민번호는 수집하지 않음. 이메일도 수집하지 않는다
- 익명 세션(쿠키 uuid) 기반, 회원가입 없음
- **서버에 개인정보를 저장하지 않는다.** 프로필은 httpOnly 쿠키에만 담고, 매칭은 그 쿠키를 읽어 처리한다. DB 에는 공공 API 에서 수집한 지원사업 데이터만 보관한다
- **프로필 쿠키는 서명이 아니라 암호화한다 (AES-256-GCM).** 서명(HMAC)은 위변조를 탐지할 뿐 내용을 가리지 못한다. 나이·성별·직업·지역·소득분위의 조합은 준식별정보인데, 서명만 붙이면 그 조합이 평문으로 클라이언트에 상주하고 매 요청 네트워크로 오간다 — "저장하지 않는다"는 주장이 "평문으로 들고 다닌다"로 상쇄된다. 키는 `SESSION_SECRET`에서 HKDF 로 파생해 세션 id 서명 키와 분리하며, GCM 이므로 기밀성과 무결성을 함께 얻는다. 세션 id 는 rate limit 키로만 쓰는 무의미한 UUID 라 서명으로 충분하다
- 저장하지 않으므로 보존기간·삭제 배치가 없다. 사용자가 브라우저 쿠키를 지우면 즉시 사라진다
- **자유입력 주의**: 사용자가 여기에 개인사(질병명, 채무 상황 등)를 적을 수 있다. 입력란에 "개인정보는 입력하지 마세요" 안내를 두고, **입력 원문은 저장하지 않는다**. 임베딩 API와 질의가 있는 최초 전체 검색의 OpenRouter에만 전송하며, OpenRouter에는 프로필·쿠키·원문 본문을 보내지 않는다
- 개인정보처리방침 페이지 필수

### 보안

- HTTPS 강제, CSP 헤더 설정
- API 키(공공데이터포털, 임베딩, OpenRouter)는 서버 환경변수. 클라이언트 노출 금지. OpenRouter 키는 Vercel 런타임에만 두고 GitHub Actions 수집 배치에는 주입하지 않는다
- 입력값 서버 측 검증 (나이 0~120, 지역코드 화이트리스트, 분위 1~10, 자유입력 200자, 결과 내 검색어 60자)
- 크롤러 외부 요청 URL 화이트리스트 고정 (SSRF 방지)
- **저장형 XSS 방어**: `title`·`summary`·`body_text`·`issuer` 는 전부 공공 API 에서 받아온 외부 텍스트이므로 HTML·스크립트가 섞여 들어올 수 있다. 렌더는 전부 JSX 보간이라 React 가 자동 이스케이프하고, `dangerouslySetInnerHTML` 은 글자크기 부트스트랩 한 곳뿐이며 그 값은 코드에 박힌 상수다. 외부 텍스트가 원시 HTML 로 들어가는 경로가 새로 생기면 테스트가 잡는다. 공고문에 섞여 오는 링크는 렌더 전에 스킴을 검사해 `http(s)` 만 통과시킨다 (`javascript:`·`data:` 차단)
- **Rate limit 이중화**: 세션 우선, IP 는 훨씬 느슨한 상한. 검색 1회 = 임베딩 API 과금 1회이므로 abuse 가 곧 비용이고, 인증이 없는 서비스라 이것이 유일한 방어선이다. 다만 **세션과 IP 에 같은 한도를 걸면 NAT 뒤 사용자들이 서로의 한도를 잡아먹는다** — 대학·회사·모바일 캐리어는 수십~수백 명이 공인 IP 하나를 공유하므로, 심사위원 여러 명이 같은 망에서 접속하면 정상 요청이 차단된다. 따라서 축을 분리한다:
  - **세션** 기본 10회/분 — 개인 단위 실질 한도
  - **익명** 기본 60회/분 — 세션 쿠키가 없는 요청의 IP 단위 한도. 개인 한도를 IP 로 대신 물리면 그것이 곧 NAT 차단이고, 이 축을 아예 빼면 쿠키를 지우는 것만으로 IP 한도를 다 쓰게 된다
  - **IP** 기본 600회/분 — 세션 쿠키를 갈아끼우는 자동화만 막는 안전망
  - 세 한도 모두 `RATE_LIMIT_SESSION_PER_MIN` / `RATE_LIMIT_ANON_PER_MIN` / `RATE_LIMIT_IP_PER_MIN` 로 덮어쓸 수 있고 매 요청 읽는다. **코드 변경·재배포 없이 값만 바꿔 심사 구간에 완화**할 수 있으며 `0` 은 해당 축 해제다
  - 차단 시에는 원인이 아니라 행동을 안내한다 ("잠시 기다렸다가 다시 시도해 주세요")
- **CSRF 방어**: 로그인이 없어도 크로스사이트에서 `POST /api/profile` 을 날려 피해자의 프로필을 바꿔치기하면 이후 결과가 통째로 달라지고, `POST /api/match` 는 임베딩 과금을 유발한다. 두 겹으로 막는다 — 쿠키의 `SameSite=Lax`(크로스사이트 POST 에 쿠키가 실리지 않는다)와 두 라우트의 `Origin`/`Sec-Fetch-Site` 검증. **비교는 스킴이 아니라 호스트로 한다** — `X-Forwarded-Proto` 를 주지 않는 환경(로컬 http, 사내망 IP)에서 스킴을 추측하면 정상 요청을 403 으로 막게 되고, CSRF 가 가려야 하는 것은 어느 사이트가 보냈는가이지 스킴이 아니다(스킴 다운그레이드는 HSTS 와 `upgrade-insecure-requests` 의 몫이다). `Origin` 이 없는 요청은 막지 않는다(구형 브라우저·프록시를 차단하게 되고, 그 경우는 SameSite 가 이미 막는다). `SameSite=Strict` 로 올리지 않는 이유는 상세 페이지 URL 공유가 설계 전제이기 때문이다
- **프롬프트 인젝션 차단**: OpenRouter 입력은 질의가 있는 최초 검색의 질의와 서버가 선택한 상위 5건의 화이트리스트 공개 메타데이터로 한정한다. 공고 메타데이터는 지시문이 아닌 데이터로 취급하도록 프롬프트에 명시하고, 응답은 한국어 평문 1,200자로 제한한다

### 신뢰성 (심사 구간 결격 방지)

- **9/7 11:00 ~ 9/11 23:59 무중단 필수.** 이 기간 배포 동결
- **인프라 무료 티어 점검 (W4, 결격 방지 항목)**: Supabase 무료 프로젝트는 비활성 시 일시정지될 수 있고, Vercel·GitHub Actions에도 무료 한도가 있다. 심야 배치가 매일 돌아 비활성 정지 가능성은 낮지만, 심사 구간을 무료 티어의 재량에 맡기지 않는다 — 심사 구간 전에 유료 플랜 전환 또는 한도·정지 정책 확인을 완료한다
- 공공 API 장애 시에도 서비스 동작 (검색은 DB를 조회한다)
- 임베딩 API 실패 시 → 집합 A만으로 렌더 (건너뛴 경로와 동일. degraded)
- **OpenRouter 장애는 답변에만 영향을 준다.** 질의가 있는 최초 검색의 호출은 1회·12초로 끝내며 재시도하지 않는다. 오류·시간 초과·키 미설정이면 최대 12초 후 답변을 unavailable로 두고 카드와 매칭 결과를 함께 응답한다
- 업타임 모니터 1개, 심사 기간 알림

### 정확성 고지

- 모든 카드 하단에 출처 URL + 수집 시각
- "본 정보는 참고용이며 최종 자격 판정은 소관 기관 확인이 필요합니다" 상시 노출
- **모든 날짜는 Asia/Seoul 고정.** `starts_at`·`ends_at` 은 시각이 없는 달력 날짜(`date`)이고, 마감일은 그 날짜의 **KST 23:59:59까지** 유효한 것으로 취급한다. DB 값을 전체 타임스탬프로 바꾸면 UTC 자정이라는 있지도 않은 시각이 생겨 KST 00:00~09:00 구간에서 D-day 가 하루 밀리고, **같은 공고가 보는 시각에 따라 D-2 로도 D-1 로도 보인다** — 사용자마다 다른 화면을 보게 되는 비일관성이다. 그래서 DB 경계에서 날짜 문자열로 정규화해 앱의 판정과 SQL 필터(`ends_at >= (now() AT TIME ZONE 'Asia/Seoul')::date`)가 같은 경계를 쓰게 한다. 오늘·어제·내일 마감 경계는 테스트로 고정한다 (§10)
- **금융소비자보호법 대응.** 이 서비스는 금감원 공시 데이터를 조건에 맞게 **비교·정보 제공**하는 것이지 특정 상품의 계약을 권유하지 않는다(판매·중개·이익 관계 없음). 실질이 그러해도 화면 문구가 "당신께 추천"처럼 읽히면 권유·광고로 해석될 여지가 생기므로, 상시 고지에 **"특정 금융상품의 가입이나 계약을 권유하지 않습니다"** 를 함께 두고, 대출·금융상품 분류에는 출처(금감원 공시)와 조건 변동 가능성을 밝히는 전용 고지를 추가한다. 화면 문구에 "추천"이라는 표현이 남지 않는지는 테스트로 검사한다

### 접근성 (포용적 금융 = 주제 정합성)

- 글자 크기 조절 토글 (고령층)
- 명도 대비 WCAG AA
- 모든 인터랙티브 요소 키보드 접근 + 레이블
- 행정용어 툴팁 설명
- 마지막 단계의 "건너뛰기"를 명확히 노출 — 입력하지 않아도 결과가 나온다는 것을 알린다

### 성능

- 결과 페이지는 임베딩 API 1회 + SQL 1회로 카드를 만들며, 질의가 있는 최초 전체 검색만 OpenRouter 답변을 추가해 응답이 최대 12초 지연될 수 있다. 답변 실패 시에도 카드 데이터는 같은 응답에 유지한다
- 입력을 건너뛴 경로(집합 A 전체)는 결과가 수백~수천 건일 수 있다 — **페이지네이션(15건 단위, 숫자 페이지) 필수.** 스코어 정렬 후 커서 기반 — 화면은 페이지 번호로 보여주되 서버는 keyset 커서만 안다
- **탭 전환은 서버를 부르지 않는다.** 커서 없는 응답에 비어 있지 않은 모든 탭의 1페이지를 함께 담는다. 탭은 이미 받은 결과를 좁히는 것뿐인데 매번 왕복하면 평범한 조작 몇 번으로 세션 rate limit(10회/분)을 태우고, 그 순간 보고 있던 결과까지 잃는다
- 동시 사용자 50명 기준

---

## 9. 화면

| # | 화면 | 내용 |
|---|---|---|
| 1 | 랜딩 | 한 문장 가치 제안 + "1분 만에 확인하기" CTA |
| 2 | 온보딩 | 6단계 (인적사항·소득 5단계 + 원하는 것 1), 진행바, 뒤로가기, 마지막 단계 건너뛰기 |
| 3 | 결과 | 매칭 요약 배너 + 질의가 있는 최초 전체 검색의 실시간 답변 + 자유입력이 가린 건수와 "전체 보기" + **결과 안에서 찾기(정렬)** + `form` 탭 + 카드 리스트 + 근접탈락 + 완화 안내 |
| 4 | 상세 | 자격 체크리스트(✅/❌), `extra_conditions` 확인 필요 항목, 금액, 기한, 신청 절차, 원문 링크 |
| 5 | 개인정보처리방침 / 출처 | 수집 소스 목록 및 갱신 주기 |

### API 초안 (기능명세서 제출물 대비)

| 메서드 | 경로 | 입력 | 출력 |
|---|---|---|---|
| POST | `/api/profile` | 정규화한 프로필 | **암호화한** 프로필 쿠키 발급. 서버 저장 없음. 월소득·가구원 수는 받지 않음. CSRF 검증 |
| POST | `/api/match` | 원하는 것 한 줄 (선택, 200자) + 탭·커서·건너뛸 페이지 수(`skipPages`) + 전체 보기 여부(`ignoreIntent`) + 결과 안에서 찾을 낱말(`sortQuery`, 선택, 60자) | **탭별** 카드 리스트(스코어순) + 근접탈락 + 적용된 완화 단계(§7.7) + 페이지 커서 + 자유입력이 가린 건수(`intentHiddenCount`) + 결과 내 정렬 적용 여부(`sortApplied`) + 적재 0건 여부(`catalogEmpty`). CSRF 검증, rate limit |
| POST | `/api/answer` | 원하는 것 한 줄 + 결과 상위 5건의 공고 id | 질의가 있는 최초 전체 검색의 실시간 AI 안내 상태/본문. 카드와 분리해 카드가 OpenRouter 를 기다리지 않게 한다. CSRF 검증, 검색과 별도 rate limit |
| GET | `/api/programs/:id` | 프로필 쿠키 (**선택**) | 상세 — 공고 정보는 항상 노출. 프로필이 있으면 자격 체크리스트(✅/❌)를 함께 돌려주고, 없으면 `checklist: null` |

**결과 안에서 찾기 (화면 3).** 결과가 수백 건일 때 사람이 찾는 것은 대개 "전세", "청년", "관악구"
같은 **낱말**인데, 순서가 §7.4 스코어 하나로만 정해져 있으면 이미 눈앞에 있는 결과를 낱말 하나로
끌어올릴 방법이 없다 — 페이지를 넘겨 가며 눈으로 찾는 수밖에 없다. 그래서 결과 화면에 낱말 입력을
두고 순서를 다시 매긴다. 두 가지를 지킨다.

- **의도 축(임베딩)을 한 번 더 걸지 않는다.** 카드에 실제로 보이는 글자(제목·요약·기관명·지원금액
  문구)에 그 낱말이 있는지만 본다. 왜 이 순서인지 화면에서 그대로 확인되고, 질의 임베딩(외부 API,
  최대 15초)을 정렬할 때마다 부르지 않아도 된다. 낱말별로 가장 앞자리 점수 하나(제목 1.0 / 요약
  0.6 / 기관 0.5 / 금액 문구 0.35)를 세어 평균 내고, 그 값이 §7.4 스코어보다 **항상 먼저** 순서를
  정한다 (§7.4 스코어는 동점일 때만 쓰인다). 점수는 0~1 이라 keyset 커서가 그대로 유효하다.
- **거르는 것이 아니라 줄을 세우는 것이다.** 걸리지 않은 공고는 아래로 내려갈 뿐 사라지지 않고,
  총 건수·탭 건수·근접탈락 구성도 그대로다. 대상인데 몰라서 못 받는 것을 없애자는 서비스가 낱말
  하나로 대상인 것을 숨기면 안 된다 (§5 "전체 보기"와 같은 이유).

낱말도 자유입력이므로 저장하지 않는다 (§8).

**프로필 없이 상세에 접근하는 경우.** 상세 페이지 URL 이 공유되는 것은 설계 전제다(수정 시 `programs.id` 유지, §3.2). 그 URL 을 연 제3자에게는 프로필 쿠키가 없으므로 자격 체크리스트를 그릴 수 없는데, 이때 에러나 빈 화면을 보이면 공유 전제가 무너진다. **공고 정보(제목·금액·기한·신청절차·원문 링크)는 그대로 노출하고, 자격 판정 영역만 "정보를 입력하면 대조해 드립니다" CTA 로 대체한다.**

자유입력은 `/api/match` 요청 시점에만 사용하고 저장하지 않는다 (§8). 모든 입력은 서버 측 검증(§8 보안)을 거친다.

---

## 10. 검증

별도 테스트 프레임워크 의존성 없이 Node 내장 `node:test`를 쓰되, **파싱과 매칭은 검증 없이 두지 않는다.**

- **정규식 파서**: 손으로 만든 자격요건 문장 41개(정상 31 + 함정 10) → `web/ingest/tests/parser.test.ts`. 함정에는 "만 65세 이상 어르신을 모시는 가구"(→ 신청자 나이 조건 아님) 같은 오탐 유발 케이스 포함
- **골든셋**: 페르소나 3종 × 기대 프로그램 목록 → 최종 결과에 들어오는지 확인. **P3(입력 건너뜀) 경로를 반드시 포함** — 상당수 사용자의 실제 경로다
- **추출 회귀**: 고정 fixture 30건을 손 라벨링해 `web/ingest/tests/pipeline.test.ts`에서 필드별 정확도 90%+를 검사한다
- **운영 추출 정확도**: API별 성공 응답에서 무작위 30건을 손 라벨링해 regex·사전 결과와 대조한다. 이는 배포 전 검증 항목이며, 미달 필드는 원문 확인 안내를 붙인다
- **교집합 재현율**: 골든셋 기대 프로그램 중 `A ∩ B`에 들어온 비율. 낮으면 top-k 확대 또는 §12-2 대응
- **수집 상태**: 배치 종료 시 소스별 건수 로그. 전일 대비 50% 이상 감소 시 알림
- **정적 기준 데이터**: 파일 값을 그대로 읽어 비교하는 테스트는 아무것도 검증하지 못한다(파일이 틀리면 테스트도 같이 틀린다). 그래서 **파일 밖의 사실**과 대조한다 — 기준중위소득은 2025년 고시 표에 발표 인상률(1인 7.20% / 4인 6.51%)을 적용한 값과, 법정동코드는 구조 불변식과 최근 개편 반영 여부(강원 51 / 전북 52 / 군위군 대구 이관)와 대조한다. 두 파일 모두 출처 URL·기준일자를 파일 안에 명시한다 ([shared/README.md](shared/README.md))
- **마감일 경계**: 오늘·어제·내일 마감 × KST 00:00 / 09:00 / 23:59 조합으로 `isOpen`·`dDay`·표기가 일관된지 검사한다 (§8 정확성 고지)
- **보안 회귀**: 외부 텍스트가 원시 HTML 로 렌더되는 경로가 없는지, 프로필 쿠키에서 평문을 읽어낼 수 없는지, 두 POST 라우트가 CSRF 검증을 거치는지, NAT 뒤 다중 사용자가 rate limit 에 걸리지 않는지
- **중복 측정**: `npm run measure:duplicates` 로 소스 간 중복률을 임계치별로 잰다. **측정 전용이며 병합하지 않는다** — 실측치를 보고 병합 구현과 소스 축소 중 어느 쪽이 싼지 결정하기 위한 것이다 (§12 리스크 표 R7)
- **A-only 경로 분포**: 자격조건이 아예 없는 소스(금감원 금융상품 등)는 `NULL = 통과` 규칙상 항상 A에 들어온다. P3 골든셋에서 범용 예·적금 상품이 상위를 도배하지 않는지 확인 — 도배되면 조건_구체성 가중치를 올린다 (§7.4)

---

## 11. 범위 (MVP)

### 포함

- 소스 T1 (보조금24 / 사회보장급여 중앙·지자체 / 기업마당 / 금감원 금융상품) + 행정표준코드 정적 사전
- 온통청년: W1 실호출 결과에 따라 T1 승격 (§3.3)
- Regex·사전 파싱 → `eligibility_rules`
- pgvector 임베딩 색인
- 6필드 입력 → 자격 ∩ 의도 교차 검증 + 스코어링 + 카드 설명(결정형 문구 + 템플릿) + 질의가 있는 최초 전체 검색 실시간 답변(§7.5)
- 결과/상세 화면, `form` 탭, 근접탈락, 빈 결과 완화
- 접근성 기본, 개인정보처리방침

### 제외 (및 추가 시점)

| 제외 항목 | 추가 시점 |
|---|---|
| 주제 분류 컬럼 / 카테고리 칩 | 자유입력만으로 의도 축이 안 잡힌다고 **측정될 때** |
| 회원가입·로그인 | 프로필 저장/이력 요구가 실제로 생길 때 |
| 마이데이터 / 건보공단 실연동 | 기관 제휴 확보 후 (로드맵으로 발표) |
| 별도 벡터 DB (Pinecone/Qdrant) | pgvector 성능이 부족하다고 측정될 때 |
| 리랭커 모델 | 교집합 정렬 품질이 부족하다고 측정될 때 |
| 자연어 질의 챗봇 | 구조화 매칭이 안정화된 후 |
| 모바일 앱 | 웹 반응형으로 충분 |
| 이메일 알림 (수집·발송 모두) | 개인정보를 서버에 저장할 이유가 생길 때. 알림을 하려면 구독자 프로필을 보관해야 하고, 그 순간 동의·보존기간·파기 의무가 따라온다. MVP 는 저장하지 않는 쪽을 택했다 |

---

## 12. 일정 (MVP 마감 2026-09-07 역산)

| 주차 | 기간 | 산출물 |
|---|---|---|
| W1 | 08-08 ~ 08-14 | **Day 1: 6개 창구 API 키 병렬 신청 + 데이터셋별 활용신청** (§3.3 — 승인 리드타임이 임계 경로) → **소스 API 실물 검증 + 임베딩 모델 한국어 성능 실측** (여기서 스펙이 바뀔 수 있음), 스키마 확정, 저장소 셋업 |
| W2 | 08-15 ~ 08-21 | 수집기 + regex·사전 파서, 첫 적재, 임베딩 색인, 파서 정확도 측정 |
| W3 | 08-22 ~ 08-28 | 교차 검증 쿼리 + 스코어링 + 골든셋 검증, 온보딩·결과 화면 |
| W4 | 08-29 ~ 09-04 | 상세 화면, 접근성, 개인정보처리방침, 배포 및 안정화 |
| 버퍼 | 09-05 ~ 09-06 | 기획서·기능명세서 PDF 작성, 최종 점검 |
| 제출 | **09-07 10:00** | 기획서 + 기능명세서 + 웹서비스 URL |
| 동결 | 09-07 ~ 09-11 | **배포 동결. 가용성 감시만** |

### 리스크 표

우선순위는 **기능 완성도가 아니라 실격 방지**다. 심사 구간(9/7 11:00 ~ 9/11 23:59)에 서비스가 죽으면 기능과 무관하게 결격이므로, 그 구간에 발현되는 항목이 먼저다.

| # | 리스크 | 발현 시점 | 영향 | 대응 | 상태 |
|---|---|---|---|---|---|
| R1 | **Rate limit 이 심사위원을 차단** — 세션·IP 동일 한도라 NAT 뒤 다중 사용자가 서로의 한도를 잠식 | 심사 구간, 같은 망에서 동시 접속 시 | 정상 요청이 429. 서비스가 죽은 것으로 보인다 | 세션(10/분)·IP(600/분) 축 분리 + 환경변수 완화 스위치(재배포 불필요) | ✅ **해결** |
| R2 | **OpenRouter 장애로 최초 검색 답변이 없음** | 심사 구간 중 rate limit·deprecation·지연 | 답변 영역이 unavailable | 12초·1회 제한, 실패 시에도 카드 데이터 유지 | ✅ **해결** |
| R3 | **콜드 스타트 — 첫 배포 시 DB 가 비어 있음** | 첫 배포 ~ 초기 적재 완료(최대 6일) | 심사위원이 빈 화면을 본다 | 적재 0건 전용 안내 화면 + 진행률 계산기 + `pg_dump` 시드 스냅샷 절차 | 🟡 **부분** — 코드는 완료, 실제 적재 여부는 미확인 |
| R4 | **마감일 하루 밀림** — `date` 컬럼을 타임스탬프로 변환해 KST 00:00~09:00 에 D-day 가 어긋남 | 상시 (자정~오전 9시) | 같은 공고가 D-2/D-1 로 갈려 사람마다 다른 화면을 본다 | DB 경계에서 날짜 정규화, 전 경로 KST 고정, 경계 테스트 | ✅ **해결** |
| R5 | **프로필 쿠키가 평문** — 서명은 위변조 탐지일 뿐 비공개가 아님 | 상시. 준식별정보가 클라이언트에 상주·전송 | 주최가 금융보안원이므로 개인정보 배점에 직결 | AES-256-GCM 암호화 + SameSite 명시 + CSRF 검증 | ✅ **해결** |
| R6 | **7인 가구 기준중위소득 값 불일치** — 고시 산정식과 40,035원 차이 | 상시, 7인 이상 가구 | 해당 가구의 소득 필터가 체계적으로 어긋남 | 검증 테스트 추가(현재 skip), 고시 원문 대조 후 확정 | 🔴 **미해결** — 사람이 고시 원문을 확인해야 함 |
| R7 | **소스 간 중복 공고** — 보조금24가 중앙·지자체복지와 커버 영역 중복 | 실적재 이후 | 카드 중복 노출, 벡터 top-k 슬롯 잠식 | 측정 도구 완비(`npm run measure:duplicates`). **병합은 실측 후 결정** | 🟡 **측정 대기** — 실데이터 필요 |
| R8 | **구조화 조건과 서술형 조건의 혼합** (W1) | 파서 구현 시점 | 자격요건 추출 누락률이 예상보다 높을 수 있음 | 전용 어댑터가 구조화 조건과 원문을 보존, 공통 파서가 보완. 표본 정확도로 스코프 조정 | 🟡 **진행 중** — 실응답 fixture 필요 |
| R9 | **의도 문장 ↔ 공고문 임베딩 신호 강도** (W1) | 벡터 검색 도입 시점 | 유사도가 무작위와 다를 바 없으면 교집합이 임의 절단 | top-k 확대 → 청킹 조정 → 벡터를 필터가 아닌 정렬 가중치로 강등(코드 경로 이미 존재) | 🟡 **진행 중** — 한국어 성능 실측 필요 |
| R10 | **무료 티어 인프라** — Supabase 비활성 정지, Vercel·Actions 한도 | 심사 구간 | 서비스 중단 = 결격 | 심사 구간 전 유료 전환 또는 한도·정지 정책 확인 | 🔴 **미착수** — 코드 아님, 계정 작업 |
| R11 | **소스 API 실물 미검증** — 전 소스가 fixture/mock 상태 | 키 발급 지연 시 | 파서 전략 전체가 흔들림. 적재 일정이 밀림 | Day 1 병렬 신청 (§3.5) | 🔴 **미착수** — 키 발급 선행 |

**두 축의 원본 리스크 (상세):**

1. **구조화 조건과 서술형 조건의 혼합 (W1).** 보조금24는 `supportConditions`로 연령·소득·가구·직업 일부를 구조화해 주지만 상세 `지원대상`·`선정기준`도 함께 제공하고, 중앙·지자체복지는 상세 자격이 주로 서술형이다. 따라서 전용 어댑터가 구조화 조건과 원문을 보존하고 공통 파서가 남은 문장을 보완한다. 남은 위험은 명세 유무가 아니라 실제 값 표기·반복 노드·누락률이므로, 소스별 성공 응답 fixture와 표본 정확도로 스코프를 조정한다.

2. **의도 문장 ↔ 공고문 임베딩 신호 강도 (W1).** 유사도가 무작위와 다를 바 없으면 교집합이 임의 절단이 된다. 대응은 단계적이다 — 먼저 top-k 확대와 청킹 전략 조정, 그래도 안 되면 벡터를 **필터가 아니라 정렬 가중치**로 강등한다(§7.3의 `JOIN` → `LEFT JOIN`, §7.4 가중치 조정). 이 경우 서비스는 "자격 통과분 전체를 의도 유사도로 정렬"이 되며, 이는 입력을 건너뛴 경로와 같은 동작이라 코드 경로가 이미 존재한다. 구조 변경 없이 흡수 가능하다.

---

## 부록: 대회 제출물 체크리스트

- [ ] 기획서 (제공 양식 → PDF 변환) — 09-07 10:00
- [ ] 기능명세서 (제공 양식 → PDF 변환) — 09-07 10:00
- [ ] 웹서비스 URL (실행 가능, 09-07 11:00 ~ 09-11 23:59 접근 보장) — 09-07 10:00
- [ ] 외부 데이터 사용 사실 공개 (대회 규정 '나'조 7-C) — 소스 목록 + 각 소스 이용허락범위 (§3.3 라이선스)
- [ ] 타 대회 제출작 재이용 아님 확인
- [ ] (본선 진출 시) 발표자료 PDF + 소스 ZIP → dacon@dacon.io, 10-08 23:59
