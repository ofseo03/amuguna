# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참조하는 컨텍스트다.
작업 시작 전 반드시 전체를 읽고, 아래 "확정된 결정"을 임의로 바꾸지 말 것.

---

## 1. 프로젝트 개요

**2026 금융 AI Challenge** (주최: 금융보안원, 운영: 데이콘) 출품작.

**서비스**: 사용자의 조건(나이·지역·직업·소득구간)에 맞는 정부·지자체 지원제도와
근거 법령을 찾아주는 웹서비스.

**핵심 가치**: 추측하지 않는다. 모든 답변에 원문 근거와 출처 링크를 함께 제시한다.

### 제약 조건

| 항목 | 값 |
|---|---|
| 팀 규모 | 2인 |
| 개발 기간 | 3주 |
| 제출 마감 | 9/7 오전 10:00 |
| URL 상시 가동 | 9/7 11:00 ~ 9/11 23:59 (접근 불가 시 결격) |
| 제출물 | 기획서 PDF, 기능명세서 PDF, 웹서비스 URL |
| 스택 | Python + FastAPI, Postgres(pgvector), Vercel |

---

## 2. 확정된 결정 (변경 금지)

### 2.1 검색은 "교집합"이 아니라 "필터 + 정렬"

가장 중요한 설계 원칙이다.

- **자격 요건 = 하드 제약** → SQL `WHERE`로 걸러낸다
- **의미 유사도 = 소프트 순위** → 걸러진 집합 안에서 `ORDER BY`

SQL 결과와 벡터 결과를 각각 구한 뒤 교집합을 취하는 구조는 **금지**한다.
벡터가 top-k만 반환하므로 교집합이 0건이 되는 상황이 실제로 발생한다.
반드시 pgvector로 단일 쿼리에서 처리한다.

```sql
SELECT *, embedding <=> :q AS dist
FROM programs
WHERE (min_age IS NULL OR :age >= min_age)
  AND (max_age IS NULL OR :age <= max_age)
  AND (region_code IS NULL OR region_code IN (:region, '00'))
  AND (deadline IS NULL OR deadline >= CURRENT_DATE)
ORDER BY dist LIMIT 10;
```

### 2.2 자격 판정은 3단계 (이분법 금지)

공고문에는 서비스가 검증할 수 없는 조건이 섞여 있다(사업자등록 여부, 재산 등).
이걸 이분법으로 처리하면 받을 수 있는 사람을 탈락시킨다.

- `ELIGIBLE` — 확인 가능한 모든 조건 충족
- `NEEDS_CHECK` — 확인 불가 조건이 남아 있음 (**기본값**)
- `INELIGIBLE` — 확인 가능한 조건 중 하나라도 위반

애매하면 `NEEDS_CHECK`로 보낸다. 잘못 배제하는 것이 잘못 포함하는 것보다 해롭다.

### 2.3 지원대상 파싱은 regex가 아니라 LLM 구조화 추출

공고문 표현이 기관마다 제각각이라 regex는 유지보수가 불가능하다.
LLM에 공고 본문을 넣고 정해진 스키마(JSON)로 추출한다.

- **오프라인 배치**로 실행 (응답 속도 무관)
- 구조화 출력 기능을 반드시 켠다 (프롬프트로 "JSON으로 답해줘"는 불충분)
- 불확실하면 값을 지어내지 말고 `null` + `unverifiable` 배열에 기록
- 필드마다 **근거 문장**(`evidence`)을 함께 추출 → 검증에도 쓰고 화면에도 노출

regex는 이미 구조화된 필드(날짜 형식 정리 등) 보조 용도로만 사용.

### 2.4 LLM은 모델 비종속으로 감싼다

`src/llm.py`의 `extract_structured()` 하나만 모델을 안다.
호출부는 모델명을 몰라야 하며, 모델 교체가 한 줄 수정으로 끝나야 한다.

- 추출용(배치): 저비용 소형 모델
- 응답용(런타임): 상위 모델
- 모델명·가격은 자주 바뀌므로 `.env`로 주입하고 코드에 하드코딩하지 않는다
- 현재 모델 목록과 가격은 https://docs.claude.com/en/docs/about-claude/models 에서 확인

### 2.5 데이터는 API 우선, 크롤링은 최후

| 순위 | 소스 | 용도 |
|---|---|---|
| 1 | 공공데이터포털 - 한국사회보장정보원 중앙부처/지자체 복지서비스 | 개인 대상 제도 |
| 2 | 기업마당 지원사업 공고 API | 기업·소상공인 지원사업 |
| 3 | 국가법령정보 공동활용 (open.law.go.kr) | 근거 법령 조문 |
| 4 | 상세 URL 본문 수집 | 위 API가 요약만 주는 경우에 한해 |

지자체 사이트 전면 크롤링은 하지 않는다.
수집 시 요청 간격 제한과 User-Agent 명시는 필수.

**API 인증키 발급에 시간이 걸린다. 최우선으로 신청할 것.**

### 2.6 개인정보는 저장하지 않는다

- 소득분위는 **사용자 자기 신고**(구간 선택). 외부 사이트 자동 연동 금지
  (마이데이터는 금융위 허가 사업, 스크래핑 대행은 불가)
- 외부 기관은 **링크 아웃**만. 로그인 정보를 우리 화면에서 받지 않는다
- 사용자 입력은 서버에 저장하지 않고 요청 처리 후 폐기
- LLM 프롬프트에 식별정보를 넣지 않는다 (연령대·지역코드 등 비식별 값만)
- 문서 업로드 기능을 넣는다면 주민등록번호 마스킹 후 즉시 삭제

### 2.7 결과 표시 규칙

- 특정 금융상품을 "추천"하지 않는다 → 제도 안내 + 원문 링크
- 모든 항목에 출처 기관명과 원문 URL을 표시 (공공누리 출처 표시 의무)
- 마감된 공고는 기본 제외
- 답변 근거가 없으면 "해당 없음"을 반환. 지어내지 않는다

---

## 3. 디렉토리 구조

```
src/
  config.py          환경변수 로딩
  models.py          Pydantic 스키마 (추출 결과, API 응답)
  db.py              Postgres 커넥션 + 쿼리
  llm.py             LLM 래퍼 (모델 비종속)
  embed.py           임베딩 생성
  extract.py         공고문 → 구조화 데이터
  match.py           SQL 하드필터 + 벡터 정렬
  api.py             FastAPI 엔드포인트
  collectors/
    base.py          수집기 공통 인터페이스
    bokjiro.py       복지서비스 API
    bizinfo.py       기업마당 API
    law.py           법령 API
scripts/
  init_db.sql        스키마 DDL
  run_ingest.py      수집 → 추출 → 적재 배치
  eval_extract.py    추출 정확도 채점
```

---

## 4. 작업 순서

### 1주차 — 데이터 파이프라인

1. 공공데이터포털·법령 API 인증키 신청 (**최우선, 지연되면 전체가 밀림**)
2. `init_db.sql` 적용, pgvector 확장 활성화
3. `collectors/` 구현 → 원본 JSON 적재
4. `extract.py` 구현 → 샘플 100건 추출
5. `eval_extract.py`로 **손으로 채점**. 정확도 모르고 전체 배치 금지
6. 전체 배치 + 임베딩 생성

### 2주차 — 서비스

7. `match.py` 단일 쿼리 구현
8. `api.py` 엔드포인트
9. 프론트엔드 (조건 입력 → 결과 카드 → 근거 원문 펼치기)

### 3주차 — 마감

10. 배포, 헬스체크 크론 설정
11. 기획서 / 기능명세서 작성
12. 예비일 (반드시 남길 것)

---

## 5. 운영 주의사항

- **DB 자동 일시정지**: 무료 티어는 비활성 시 잠들 수 있다.
  8월 말부터 헬스체크 크론(GitHub Actions)으로 주기적 쿼리를 날린다.
  심사 기간 중 접근 불가는 결격 사유다.
- **커넥션 풀러 사용**: 서버리스 환경에서는 직접 연결 URL이 아니라 풀러 URL을 쓴다.
- **API 호출 한도**: 런타임에 공공 API를 직접 부르지 않는다.
  배치로 적재한 자체 DB만 조회한다.
- **Vercel Python 제약**: 실행 시간·번들 용량 제한이 있다.
  무거운 배치는 로컬/별도 워커에서 돌리고, 서비스는 조회만 담당한다.

---

## 6. 코딩 규칙

- Python 3.11+, 타입 힌트 필수
- 스키마 정의는 Pydantic. dict를 그대로 흘리지 않는다
- 외부 호출(API·LLM·DB)은 재시도와 타임아웃을 넣는다
- 비밀키는 `.env`. 커밋 금지
- 로그에 사용자 입력값을 남기지 않는다
- 수집 스크립트는 멱등하게 (같은 공고 재수집 시 중복 생성 금지, `source_id` UPSERT)

---

## 7. 대회 규칙 관련

- 외부 데이터 사용 시 **출처와 수집 방법을 기획서에 공개**해야 한다 (대회 규칙 제7조 C항)
- 타인의 코드·데이터 무단 사용 시 수상 취소
- 제출 마감 후 산출물 수정 불가
- 발표는 PDF로만 진행 (PPT 불가)
