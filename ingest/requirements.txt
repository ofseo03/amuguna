# amuguna 수집 배치 런타임 의존성 (SPEC §4 기술 선택)
#
# 필수 —
httpx>=0.27,<1.0
selectolax>=0.3.21        # T3 크롤링(HTML) 보완용 파서

# 선택 (전부 import-guarded) —
# 없어도 패키지가 import 되고 테스트가 통과한다. 각각 아래 경로에서만 필요하다.
#   psycopg   : 실 Postgres 적재 (--dry-run 없이 실행할 때)
#   anthropic : 자격요건 LLM 보완(§6.2) + 요약·절차 생성(§7.5)
# 둘 다 없으면 InMemoryDatabase + mock 요약으로 강등된다.
psycopg[binary]>=3.1,<4.0
anthropic>=0.39
